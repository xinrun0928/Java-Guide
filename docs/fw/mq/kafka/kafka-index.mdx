---
title: Kafka 索引机制与零拷贝
description: 详解 Kafka 的稀疏索引设计、零拷贝技术（mmap/sendfile），以及为什么 Kafka 能做到高吞吐低延迟
---

# Kafka 索引机制与零拷贝

> 上一节 [Kafka 存储机制与日志分段](/fw/mq/kafka/storage) 提到了 `.index` 文件，这节深入解析索引和零拷贝。

## 稀疏索引

Kafka 使用稀疏索引，不是每条消息都建索引：

```properties
# 索引条目间隔（默认 4096 条消息一个索引项）
index.interval.bytes=4096
```

### 索引查找流程

假设要读取 offset=1000 的消息：

```mermaid
sequenceDiagram
    participant C as Consumer
    participant I as Index
    participant L as Log

    C->>I: 查找 offset=1000
    I->>I: 二分查找最近的索引项<br/>offset=992 -> position=40960
    I-->>L: 定位到 position=40960
    L->>L: 从 position 开始顺序扫描
    L-->>C: 返回 offset=1000 的消息
```

**稀疏索引的优势**：空间小，查找快。

## 零拷贝技术

传统数据读取 vs 零拷贝：

```mermaid
flowchart LR
    subgraph "传统 IO (4次拷贝)"
        D1[磁盘]
        K1[内核缓冲区]
        U1[用户缓冲区]
        N1[网卡缓冲区]

        D1 -->|1. 磁盘→内核| K1
        K1 -->|2. 内核→用户| U1
        U1 -->|3. 用户→内核| K1
        K1 -->|4. 内核→网卡| N1
    end

    subgraph "零拷贝 (2次拷贝)"
        D2[磁盘]
        K2[内核缓冲区]
        N2[网卡缓冲区]

        D2 -->|1. 磁盘→内核| K2
        K2 -->|2. 内核→网卡| N2
    end
```

### mmap（内存映射）

```java
// Linux: 将文件映射到内存
FileChannel.map(FileChannel.MapMode.READ_WRITE, 0, fileSize);

// 读取时：内存直接访问，避免 read/write 系统调用
// 写入时：直接写内存，由 OS 刷盘
```

### sendfile

```java
// 内核空间直接转发，避免用户态拷贝
FileChannel.transferTo(position, length, socketChannel);
```

Kafka 消费者获取消息时，走的是 `sendfile` 路径：

```bash
# 查看 Kafka 使用的 IO 方式
strace -e trace=sendfile -p <kafka_pid>
```

## PageCache 加速

Linux 的 PageCache 会缓存热点数据：

```mermaid
graph TB
    subgraph "Memory"
        PC[PageCache]
        B[应用 Buffer]
    end

    subgraph "Disk"
        F[文件系统]
    end

    F -->|读取| PC
    PC -->|命中| B
    PC -.->|未命中| F
```

**写入**：先写 PageCache，异步刷盘
**读取**：热点数据在内存，直接返回

## 面试高频追问

**Q：为什么 Kafka 比传统数据库快？**

A：从 IO 路径看，传统数据库：
1. 写操作要刷 redo log（顺序写）
2. 写操作要更新索引（随机写）
3. 数据和索引都在磁盘

Kafka：
1. 只有顺序写
2. 没有索引更新
3. 利用 PageCache 热点缓存
4. 零拷贝减少 CPU 开销

**Q：Kafka 的缺点是什么？**

A：
1. 消息堆积会占用大量磁盘
2. 过期消息清理有 IO 开销
3. Topic 数量过多时元数据压力大

---

*存储与索引是性能基石，下一节看 Broker 层面如何协调：[Kafka 控制器选举](/fw/mq/kafka/controller)*
